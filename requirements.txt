requests>=2.31.0
python-dotenv>=1.0.0
numpy>=1.26.0
trimesh>=4.0.0
Pillow>=10.0.0
moderngl>=5.10.0
pyglet>=2.0.0

# Server worker only (object storage + job queue) — not needed for desktop/CLI use
boto3>=1.34.0
redis>=5.0.0
rq>=1.16.0

# Desktop API service (FastAPI backend for the Tauri + React UI)
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
