# Server worker image deps only. Excludes desktop-only packages from
# requirements.txt (moderngl, pyglet — core/gl_renderer.py's GL preview
# viewer, only imported by desktop_app.py, never by main.py/service/worker.py).
requests>=2.31.0
python-dotenv>=1.0.0
numpy>=1.26.0
trimesh>=4.0.0
Pillow>=10.0.0
boto3>=1.34.0
redis>=5.0.0
rq>=1.16.0
